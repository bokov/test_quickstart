list("data/example_data_pbc.csv" # smartfilechoose(batchmode = "data/example_data_pbc.csv", autoresponse = pop("confchfile"))
,1 # smartmenu(choices = c("Select an additional file.", "Change the name of a variable.",      "Unselect one of the files.", "Save selections and continue."),      batchmode = 4, autoresponse = pop("confchfile"), title = "What do you wish to do?",      extramessage = {         ui_line("\n\nThese are the files you have chosen and the variable names to which they will\n be assigned after getting imported into R:\n")         for (ii in names(.inputdata)) {             ui_line("{ui_field(ii)}\t{ui_path(.inputdata[ii])}")         }     })
,"data/example_data_pbc.csv" # smartfilechoose(batchmode = "data/example_data_pbc.csv", autoresponse = pop("confchfile"),      ignorecancel = FALSE)
,1 # smartmenu(choices = c("Select an additional file.", "Change the name of a variable.",      "Unselect one of the files.", "Save selections and continue."),      batchmode = 4, autoresponse = pop("confchfile"), title = "What do you wish to do?",      extramessage = {         ui_line("\n\nThese are the files you have chosen and the variable names to which they will\n be assigned after getting imported into R:\n")         for (ii in names(.inputdata)) {             ui_line("{ui_field(ii)}\t{ui_path(.inputdata[ii])}")         }     })
,"data/example_data_pbc.csv" # smartfilechoose(batchmode = "data/example_data_pbc.csv", autoresponse = pop("confchfile"),      ignorecancel = FALSE)
,3 # smartmenu(choices = c("Select an additional file.", "Change the name of a variable.",      "Unselect one of the files.", "Save selections and continue."),      batchmode = 4, autoresponse = pop("confchfile"), title = "What do you wish to do?",      extramessage = {         ui_line("\n\nThese are the files you have chosen and the variable names to which they will\n be assigned after getting imported into R:\n")         for (ii in names(.inputdata)) {             ui_line("{ui_field(ii)}\t{ui_path(.inputdata[ii])}")         }     })
,3 # smartmenu(choices = .inputdata, batchmode = 0, autoresponse = pop("confchfile"),      title = "Which file do you wish to un-select?", extramessage = {         ui_line("The file you unselect will not be touched, just its data will not be imported \ninto this project. Type {ui_code(0)} to not select any files and return to the \nprevious menu.")     }, ignorezero = FALSE)
,2 # smartmenu(choices = c("Select an additional file.", "Change the name of a variable.",      "Unselect one of the files.", "Save selections and continue."),      batchmode = 4, autoresponse = pop("confchfile"), title = "What do you wish to do?",      extramessage = {         ui_line("\n\nThese are the files you have chosen and the variable names to which they will\n be assigned after getting imported into R:\n")         for (ii in names(.inputdata)) {             ui_line("{ui_field(ii)}\t{ui_path(.inputdata[ii])}")         }     })
,2 # smartmenu(choices = .inputdata, batchmode = 0, autoresponse = pop("confchfile"),      title = "Which variable do you wish to rename?", extramessage = {         ui_line("After you enter in the name you want it will get modified for uniqueness, \ncompatibility with R and ease of typing. Names starting with {ui_code('dat')} \nwill be renumbered. Type {ui_code(0)} to cancel and go back to previous menu.")     }, ignorezero = FALSE)
,"bar foo" # smartreadline(prompt = sprintf("Type in a new name for '%s': ",      names(.inputdata)[.torename]), autoresponse = pop("confchfile"))
,4 # smartmenu(choices = c("Select an additional file.", "Change the name of a variable.",      "Unselect one of the files.", "Save selections and continue."),      batchmode = 4, autoresponse = pop("confchfile"), title = "What do you wish to do?",      extramessage = {         ui_line("\n\nThese are the files you have chosen and the variable names to which they will\n be assigned after getting imported into R:\n")         for (ii in names(.inputdata)) {             ui_line("{ui_field(ii)}\t{ui_path(.inputdata[ii])}")         }     })
)